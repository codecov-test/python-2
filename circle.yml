machine:
  python:
    version: 2.7.8

dependencies:
  override:
    - pip install codecov
    - pip install -r requirements.txt

test:
  override:
    - nosetests --with-coverage
  post:
    - codecov
